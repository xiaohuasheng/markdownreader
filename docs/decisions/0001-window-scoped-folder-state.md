# 0001：文件夹状态按窗口隔离

## 背景

阅读器此前只有一个全局“当前窗口”和一套文件夹监听状态。打开第二个文件夹会覆盖第一个窗口的侧栏与监听器，无法并行阅读多个目录。

## 决定

每个 Electron 窗口独立保存文件夹监听器、刷新定时器与对应路径。来自菜单和渲染进程的打开、保存操作按焦点窗口或 IPC 发起窗口路由；“Open Folder in New Window”会创建窗口后只向该窗口发送打开事件。

## 后果

多个窗口可同时浏览不同目录，并各自响应外部文件变化。窗口关闭或应用退出时会关闭其监听器，避免泄漏文件描述符。全局最近打开列表仍保持共享。
